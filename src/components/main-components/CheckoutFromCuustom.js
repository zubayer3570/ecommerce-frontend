import React from 'react';

const CheckoutFromCuustom = () => {
    const handleSubmit = (e) => {
        e.preventDefault()
    }


    return (
        <form onSubmit={handleSubmit}>

        </form>
    );
};

export default CheckoutFromCuustom;